# exp-000-baseline — intentionally empty, reused from turkish-capability

This experiment line does not re-run an untouched-model baseline. It reuses
`experiments/turkish-capability/qwen3.5-4b/exp-000-baseline`'s Phase 2
generation results directly — see the parent `README.md`'s "Baseline
(reused, not re-run)" section for the numbers and a pointer to the full
evidence.
